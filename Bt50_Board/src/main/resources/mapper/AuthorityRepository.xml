<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.repository.AuthorityRepository">
    <select id="findByName" resultType="com.lec.spring.domain.Authority">
        SELECT
            id "id"
             , name "name"
        FROM t5_authority
        WHERE name = #{name}
    </select>

    <select id="findByUser" parameterType="com.lec.spring.domain.User" resultType="com.lec.spring.domain.Authority">
        SELECT a.id "id", a.name "name"     # authority a의 id를 "id"로
        FROM t5_authority a, t5_user_authorities u
        WHERE a.id = u.authority_id AND u.user_id = #{id};  # user 의 id 값
    </select>

    <insert id="addAuthority" flushCache="true">
        INSERT INTO t5_user_authorities(user_id, authority_id)
        VALUES(#{param1}, #{param2})    # 하나만 필요할 때에는 VALUE 여러개는 VALUES
    </insert>

</mapper>
